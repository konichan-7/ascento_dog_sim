# 动态性能增强路线图（improve.md）

本文是**规划文档**，与描述现状的 `docs/design.md` 互补：`design.md` 记录"当前代码实际采用什么"，本文记录"下一步要做什么、怎么做"。所有改进项按 `AGENTS.md` 的工程约定推进：控制/估计逻辑与 MuJoCo 解耦（纯 numpy 放 `ascento_dog/control/`，MuJoCo 侧只做 I/O）、每项配确定性测试、落地后回流文档。

现状基线（动态性能相关）：

- 整机质量 $M=20.65\ \mathrm{kg}$（底盘 17.82 kg；每条腿可动部分约 0.57 kg：AD 0.063 + DCE 0.077 + BC 0.065 + 轮 0.365）；
- 执行器示意限制：髋 $\pm40\ \mathrm{N\,m}$、单腿支撑力 $\le120\ \mathrm N$、轮力矩输出限 $20\ \mathrm{N\,m}$（控制层）；
- 控制现状：VMC 只闭环高度与 roll/pitch（`QuadrupedVMC`）；平动与 yaw 开环（轮速 PI 跟踪设定值）；跨台阶另用位置伺服状态机；
- 状态来源：直接读 MuJoCo 真值（`read_vmc_state`）；IMU 传感器（`imu_quat`/`imu_gyro`/`imu_accel`）已存在但未接入控制；
- 全部执行器/摩擦/接触参数为示意值，未经辨识。

动态性能增强的总体思路是**逐步拆除准静态假设**。按依赖关系分四层，前置为参数辨识：

| 优先级 | 层 | 目标 | 主要依赖 |
| --- | --- | --- | --- |
| P0 | 参数辨识 | 执行器、摩擦、接触参数有实测值 | — |
| P1 | 框架内补动力学 | 惯性/科氏前馈、平动+yaw 闭环、加减速姿态前馈、状态估计 | P0（增益与模型） |
| P2 | 全接触力分配 | 竖直力模型升级为 6D wrench + 摩擦锥 | P1 的状态估计 |
| P3 | 全身动力学控制 | 把腿/闭链动力学纳入力矩求解 | P2 |
| P4 | 动态规划与运动生成 | 冲量—动量、跳跃轨迹、MPC | P3 的跟踪带宽 |

---

## P0　参数辨识（前置）

**现状**：髋执行器被当作理想力矩源；轮地摩擦、腿惯量全部用示意值。动态控制对模型精度敏感，必须先辨识，否则一切"动态增强"都是在错误模型上调参。

**实施**：

1. **单腿惯量与摩擦辨识**（仿真 + 实物同协议）：
   - 底盘锁定，对单髋施加激励力矩序列（阶跃 + 多频正弦扫频），记录 $q,\dot q,\ddot q$（`read_hip_rates` + 数值差分或 `mj_forwardAcc`）；
   - 摩擦模型 $\tau_f=b\dot q+f_c\operatorname{sign}(\dot q)$：低速匀速段测库仑项 $f_c$，不同速度段拟合粘滞 $b$；
   - 等效惯量 $I_{eff}(q)=\dfrac{\tau_{cmd}-\tau_f}{\ddot q}$ 沿 $q\in[-65^\circ,-15^\circ]$ 采样建表（数值上可由 MuJoCo `mj_forwardAcc` 或能量法 $\tfrac12I_{eff}\dot q^2$ 与动能核对）；每条腿对称，共测一条；
   - 轮子同理：轮惯量 + 轮-地滚动阻力。
2. **执行器动态**：髋力矩指令到实际力矩的一阶/二阶延迟模型（带宽 $T_{act}$），跨台阶/高速场景必须补偿。
3. **轮地接触**：静/动摩擦系数 $\mu_s,\mu_d$、接触刚度（MuJoCo `solref` 对应量）——用轮牵引滑移实验标定。
4. **产出**：`ascento_dog/control/actuator_model.py`（纯数据类 + 拟合参数）；文档回流 `docs/design.md` 参数表，替换所有"示意值"标注。

---

## P1　框架内补动力学（近期，仿真可落地）

### 1.1 髋力矩惯性/科氏前馈

**现状**：`QuadrupedVMC`/跨台阶位置伺服输出 $\tau=-J^{\mathsf T}F$ 或 $\tau=K_p(q^*-q)-K_d\dot q$，无腿惯性补偿；高速摆动（>5 rad/s）时腿惯性力矩可达数 N·m。

**实施**：

- 新建 `ascento_dog/control/leg_dynamics.py`（与 MuJoCo 解耦）：
  - `LegInertiaModel`：由 P0 的表拟合 $I_{eff}(q)$（多项式/样条），提供 $I_{eff}(q)$ 与 $\tfrac12\dfrac{dI_{eff}}{dq}$（能量法科氏项）；
  - 前馈 $\tau_{ff}=I_{eff}(q)\ddot q_d+\tfrac12\dfrac{dI_{eff}}{dq}\dot q^2$；
- 接入点：跨台阶位置伺服（`step_crossing.py` 的 `_servo_torque`）先加（摆动大）；VMC 的支撑映射暂不加（低速支撑为主）；
- **验证**：`tests/test_leg_dynamics.py`——合成 $q(t)$ 轨迹下前馈与实际所需力矩（MuJoCo 中固定底盘测反力）残差 < 门槛；跨台阶全程髋力矩峰值下降统计。

### 1.2 平动速度闭环与 yaw 姿态闭环

**现状**：轮速 PI 只跟踪开环设定值（`wheel_speed_targets` 由 `TeleopCommand` 生成）；机体前向速度与 yaw 偏差不反馈。任何加减速/转向的"动态"都无闭环保证。

**实施**：

- 扩展 `VMCState`：新增 `linear_velocity_body: Vector3`（`read_vmc_state` 已调用 `mj_objectVelocity` 取 6 维局部速度，现只用了角速度前三维，补齐即可）；
- 新建 `ascento_dog/control/horizontal_control.py`：
  - 速度环：$v_x^{cmd}=K_{pv}(v_x^*-v_x^{meas})+v_x^*$（跨台阶 `REAR_CLIMB` 等需压低增益避免与位置模式冲突）；
  - yaw 环：$\omega_{yaw}^{cmd}=K_{py}(\chi^*-\chi)-K_{dy}\omega_z+\omega_{yaw}^*$；
  - 输出仍为 `TeleopCommand`，复用现有 `wheel_speed_targets` 差速映射（零新运动学代码）；
- 接入 `step_teleop` 与跨台阶轮速分支，保留开环开关做 A/B 对比；
- **验证**：`tests/test_mujoco_teleop.py` 扩展——直线 3 m/s 加速段速度误差 < 5%；阶跃 yaw 指令超调/稳态角 < 门槛；新 `tests/test_horizontal_control.py` 纯逻辑测试。

### 1.3 加减速俯仰前馈

**现状**：轮驱动/制动在接触点（质心下方）产生俯仰力矩（加速抬头），当前全靠 pitch PD 事后纠正，动态加减速时姿态瞬态大。

**实施**：

- `QuadrupedVMC.compute` 增加可选前馈参数 `forward_acceleration` 与系数 $K_{ff}$：$\tau_y^*\mathrel{+}=K_{ff}\,a_x$（符号按抬头方向标定；量级 $\approx M a_x h_{com}$ 起步）；
- 前馈源：`linear_velocity_body` 差分（P1.2 已有）或状态估计器（P1.4）；
- **验证**：`tests/test_vmc.py` 新增加减速扰动工况，对比开/关前馈的 pitch 峰值与恢复时间。

### 1.4 状态估计器（EKF）

**现状**：控制直接读 MuJoCo 真值（`read_vmc_state`），`docs/design.md` §4.4 已声明无传感器融合——实物化的硬前置。

**实施**：

- 新建 `ascento_dog/control/estimator.py`（纯 numpy EKF）：
  - 状态：机体位置/线速度/姿态四元数/角速度 + 陀螺零偏；预测用 IMU 角速度与线加速度（`imu_gyro`/`imu_accel` 已存在）；
  - 观测：轮速里程计（`read_wheel_velocities`→平面运动约束）与腿高观测（FK 轮心高度 vs 里程计预测）；
  - 仿真验证用真值加噪声合成测量，真值路径保留开关；
- **验证**：`tests/test_estimator.py`——静止/直线/转弯三类轨迹的位置、速度、姿态误差统计；噪声敏感性。

---

## P2　全接触力分配（中期架构升级）

**现状**：`allocate_vertical_forces` 只有竖直力通道（3×4 分配，`roll_axis/pitch_axis` 裁剪是单轴支撑的补丁）；水平力（加速、爬坡）无法表达，跨台阶才被迫切位置伺服绕开。

**澄清：单自由度腿为何能参与全接触力分配**（消除"腿只有 1 DoF 怎么给二维力"的疑问）：

- 接触力是**物理量**（由系统平衡决定），不是分配器直接"塞"进腿的；控制器通过每个接触点的**两个执行器**影响它——髋力矩与轮力矩。
- 每个接触点的准静态分工：髋力矩控制接触力**竖直**分量（$\tau_h=-J_z(q)F_z$），轮力矩控制**水平**分量（$F_x=\tau_w/R$，无滑滚动）。接触点执行器组是"腿+轮"，两个输入对应矢状面两个力分量，自由度足够；若没有轮、纯 1-DoF 足式腿，才真的只能做竖直力分配——这正是当前 3×4 分配只处理竖直力的根源。
- 提升不在于"腿变出二维力"，而在于把被闲置的水平通道（轮，当前是速度 PI，不参与力分配）请进同一个优化：竖直支撑与水平牵引在摩擦锥内协同。

**目标（修正）**：统一表达**平地/缓坡轮地接触**下的水平动力学（加速/制动/转向/斜坡侧倾的载荷转移与摩擦利用）。跨台阶的立面接触（法向水平、接触点在轮心侧面）需要接触几何/法向估计，**状态机回归统一框架列为后续延伸目标，不作为 P2 承诺**。

**实施**：

- 新建 `ascento_dog/control/contact_forces.py`：
  - 每腿接触力 $f_i\in\mathbb R^3$，目标 wrench 6D $w=[F_x,F_y,F_z,\tau_x,\tau_y,\tau_z]$，接触点取轮心、法向取地形法向（平地 = $e_z^W$，扩展为接触面估计）；
  - 摩擦锥线性化：每腿一组锥不等式（如 4 面锥 $|f_{i,t}|\le\mu f_{i,n}$）+ 力边界；
  - 小规模 QP（变量 4×3=12，约束每腿 4–8 条）——仓库依赖只有 numpy：**先评估手写有效集/内点法**（可复用 `allocate_vertical_forces` 的枚举思想升级），否则与用户确认引入求解器依赖；  - 分配结果与现有竖直分配在零水平目标、水平机体下应一致（向后兼容测试）；
- 上层打通：P1.2 速度环输出 $F_x^*$、P1.3 姿态环输出 $\tau_y^*$，进入 6D wrench；`QuadrupedVMC` 提供开关切换 3D/6D 分配；
- **验证**：`tests/test_contact_allocation.py`（精确恢复、锥内、边界饱和、与旧分配一致性）；`test_vmc.py` 水平加速工况下不再依赖轮速开环也能维持速度。

### 部署约束（目标平台 STM32H723）

最终算法将部署到 STM32H723（Cortex-M7 @ 550 MHz，单精度 FPU，无硬件双精度）。P2 的 QP 与后续 P3 的求解器设计必须从第一天起满足该平台约束：

- **求解器选型**：Hildreth 投影法或有效集法，C 实现、固定数组、无动态内存分配；双精度一律不用（单精度，注意力/力矩量纲缩放与容差放宽到 $10^{-4}$ 量级）；
- **确定性**：固定迭代次数上限保证最坏执行时间有界，满足实时控制周期；
- **频率**：控制率定为 500 Hz–1 kHz，QP 预算 ≤ 300 μs，其余留给 EKF、运动学与通信（详见下方可行性小节）；
- Python/numpy 实现仅用于离线验证与参数生成，板上运行 C 移植版本，两者以相同测试向量交叉核对。

QP 在 H723 上的可行性分析见文末"部署可行性：STM32H723 上的 QP 预算"一节。

---

## P3　全身动力学控制（远期，需 P2 与辨识数据）

**目标**：把腿/闭链动力学从"被忽略的扰动"变成控制器内的一阶公民。

**实施路径（分两步，避免一步到位的复杂度）**：

1. **单刚体模型（SRBM）力控**：质心动力学 $m\ddot r_{com}=\sum f_i+mg$、$I\dot\omega+\omega\times I\omega=\sum(r_i\times f_i)$ 作为规划/前馈模型；接触力由 P2 的 QP 求解；腿仍经雅可比映射——此步等效于"第一层 + 6D 接触力 + 质心轨迹前馈"；
2. **关节级 WBC**：任务空间（接触力/质心加速度）为主任务、姿态/高度为次任务、零空间加关节顺应；对 1-DoF 闭链腿需要把闭链约束（两个 $C$ 点重合）纳入动力学（约束消元或投影），髋力矩与轮力矩在接触点合成（腿给竖直分量、轮给水平分量）——闭链的动力学建模是此步主要工作量。

**验证**：新增 `tests/test_wbc.py`；高速扫摆、急停、上台阶动态工况下姿态/高度跟踪误差显著低于第一层基线。

---

## P4　动态规划与运动生成（远期）

**目标**：把准静态状态机升级为利用动量的运动生成（Ascento 式"冲上台阶"、跑跳、高速越障）。

**实施**：

- **离线轨迹**：冲量—动量规划（给定起跳/落地状态解接触冲量）、跳跃与高速越障轨迹离线生成存表（纯 numpy）；
- **在线 MPC**：以 P3 的 SRBM 为预测模型做滚动时域优化（控制周期 50–100 ms 量级），替换跨台阶状态机的分段设定值；需与用户确认求解器策略（自研 vs 引入依赖）；
- 落地顺序：先做"动态上台阶"单一场景（现有 `step_crossing.py` 的 `APPROACH` 速度/姿态预前倾 + 冲量跳跃），验证后推广。

---

## 横向工程规范

1. **解耦**：所有算法（惯性模型、估计器、水平环、QP 分配、规划器）放 `ascento_dog/control/`（或 `ascento_dog/planning/`），纯 numpy；`ascento_dog/simulation/` 只加 I/O 与真值/噪声测量合成；
2. **文档同步**：每完成一项，把"现状"部分从本文移入 `docs/design.md`（或对应功能文档），并同步 `AGENTS.md` 的功能分层；
3. **验证门槛**：每项落地必须过 `uv run ruff check`、`uv run ruff format --check`、`uv run pytest`；新增测试文件与本文编号对应（`test_leg_dynamics.py`、`test_estimator.py`、`test_contact_allocation.py`、`test_wbc.py`）；
4. **参数纪律**：P1 之后所有新增增益/限幅写入参数类并注明来源（辨识值/示意值），不允许散落魔法数；
5. **A/B 基线**：每层保留开关，与前一层的确定性场景对比（同一仿真种子），避免"看起来更动感但指标变差"。

---

## 风险与开放问题

- 求解器策略已受 H723 平台约束收敛（Hildreth/有效集、C、单精度、固定迭代）——剩余决策点是 Python 原型与 C 移植的交叉验证流程；
- 闭链腿的动力学建模（P3）无现成解析路径，需要先用数值（`mj_forwardAcc`/`mj_rne`）标定再决定解析实现深度；
- 实物参数缺失使 P1 的增益整定仍是示意——P0 不完成，P1–P4 的"性能"结论只对仿真成立；
- 跨台阶状态机（位置伺服）与 P1/P2 力控框架并存期间的接口统一（`CrossingCommand` vs 新 wrench 接口）需在 P2 中设计，避免两套指令格式长期分叉；
- H723 单精度下 QP 的病态风险（力/力矩量纲混合、摩擦锥浅角）：用与 `W_A` 相同的量纲归一缩放，并纳入交叉验证测试向量。

---

## 部署可行性：STM32H723 上的 QP 预算

**平台**：STM32H723 = Cortex-M7 @ 550 MHz，单精度 FPU（VFPv5，硬件无双精度，double 走软件模拟慢一个量级），片上 SRAM 数百 KB，无外部内存依赖需求。

**问题规模**（P2 全接触力分配）：变量 12（4 腿 × 3 力分量，矢状面对称时可按 8–10 变量裁剪），等式 6（wrench 匹配，或按残差进目标），不等式 16–40（每腿 4–8 面摩擦锥 + 力上下界）。

**计算量估算**（单精度）：

- Hildreth 投影：每轮主循环 $\approx m\times 2n$ 次乘加（$n\approx12,\ m\approx30$）≈ $10^3$ flops，收敛 50–200 轮 → $10^4$–$2\times10^5$ flops；
- 有效集法：每外迭代解一次 ≤12 维法方程（Cholesky ≈ $n^3/3$）≈ $10^3$–$10^4$ flops，激活集切换 10–30 次 → $10^4$–$3\times10^5$ flops；
- 550 MHz × 1 MAC/cycle 理论 $5.5\times10^8$ flops/s，FPU 实际吞吐约 30–60%，即 $2$–$3\times10^8$ flops/s；
- 单次 QP 估算 $10^5$ flops 量级 → **50–300 μs**（含循环/地址开销放宽 2–4 倍）。

**结论**：

- **可行**。12 变量小 QP 在 1 kHz（1 ms）预算内约占 5–30% CPU；控制率取 500 Hz–1 kHz 均从容，留出 EKF、运动学与通信余量；
- 无约束/少约束工况（正常行驶，锥不激活）退化为解析最小范数解 + 标量投影，μs 级快路径，QP 只在饱和/打滑边缘激活——这是实际占空比的主要形态；
- 同类小 QP（Hildreth 投影）在嵌入式控制器上运行足式/轮腿力分配有公开先例，工程风险可控；
- 必须遵守：单精度 + 量纲归一缩放 + 容差 $10^{-4}$ 量级、固定迭代上限保证最坏执行时间、C 实现零动态分配；Python 原型与 C 移植用同一组测试向量交叉核对（含锥边界激活与病态缩放用例）。
